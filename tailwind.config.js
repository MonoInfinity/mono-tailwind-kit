const theme = require("tailwindcss/defaultTheme");

module.exports = {
        // mode: "jit",
        content: ["./build/index.html"],

        theme: {
                extend: {},
        },
        variants: {
                extend: {},
        },
        plugins: [],
};
